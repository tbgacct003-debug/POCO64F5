<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Top Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:background="#000000"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/btnFlash"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="⚡"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:gravity="center" />

        <View android:layout_width="0dp" android:layout_height="1dp" android:layout_weight="1" />

        <TextView
            android:id="@+id/badge64M"
            android:layout_width="wrap_content"
            android:layout_height="24dp"
            android:text="64M RAW"
            android:textColor="#FFCC00"
            android:textStyle="bold"
            android:textSize="10sp"
            android:gravity="center"
            android:paddingHorizontal="8dp"
            android:background="@drawable/bg_ios_badge" />
    </LinearLayout>

    <!-- 4:3 Viewfinder Container -->
    <FrameLayout
        android:id="@+id/viewfinderContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="3:4"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextureView
            android:id="@+id/textureView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- iOS Yellow Reticle + Sun Exposure Adjustment -->
        <RelativeLayout
            android:id="@+id/focusBox"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:visibility="gone">

            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_ios_focus_square" />

            <ImageView
                android:id="@+id/exposureSun"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginEnd="-26dp"
                android:src="@android:drawable/ic_menu_day"
                android:tint="#FFCC00" />
        </RelativeLayout>
    </FrameLayout>

    <!-- Zoom Bubble (0.6x, 1x, 2x) -->
    <LinearLayout
        android:id="@+id/zoomSelector"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:orientation="horizontal"
        android:background="@drawable/bg_ios_zoom_pill"
        android:gravity="center_vertical"
        android:paddingHorizontal="4dp"
        android:layout_marginBottom="12dp"
        app:layout_constraintBottom_toTopOf="@id/modeCarousel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/btnZoomWide"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:text=".6"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:id="@+id/btnZoom1x"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:text="1×"
            android:textColor="#FFCC00"
            android:textSize="12sp"
            android:textStyle="bold"
            android:background="@drawable/bg_ios_circle_active"
            android:gravity="center"
            android:layout_marginHorizontal="3dp" />

        <TextView
            android:id="@+id/btnZoom2x"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:text="2"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:gravity="center" />
    </LinearLayout>

    <!-- Carousel Modes -->
    <HorizontalScrollView
        android:id="@+id/modeCarousel"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:scrollbars="none"
        android:layout_marginBottom="12dp"
        app:layout_constraintBottom_toTopOf="@id/bottomControls">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="140dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="VIDEO"
                android:textColor="#8E8E93"
                android:textSize="12sp"
                android:textStyle="bold"
                android:layout_marginEnd="20dp" />

            <TextView
                android:id="@+id/modePhoto"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="PHOTO"
                android:textColor="#FFCC00"
                android:textSize="12sp"
                android:textStyle="bold"
                android:layout_marginEnd="20dp" />

            <TextView
                android:id="@+id/modePortrait"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="PORTRAIT"
                android:textColor="#8E8E93"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Bottom Controls -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottomControls"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:background="#000000"
        android:paddingBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageView
            android:id="@+id/galleryThumb"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="36dp"
            android:background="@drawable/bg_ios_thumbnail"
            android:clipToOutline="true"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <FrameLayout
            android:id="@+id/captureBtn"
            android:layout_width="74dp"
            android:layout_height="74dp"
            android:background="@drawable/bg_ios_shutter_outer"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <View
                android:id="@+id/shutterInner"
                android:layout_width="62dp"
                android:layout_height="62dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_ios_shutter_inner" />
        </FrameLayout>

        <ImageButton
            android:id="@+id/btnFlip"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="36dp"
            android:background="@drawable/bg_ios_circle_translucent"
            android:src="@android:drawable/ic_menu_rotate"
            android:tint="#FFFFFF"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
